# Netlify configuration for the Morgan Family Recipe Box

[build]
  publish = "."
  functions = "netlify/functions"
  # Force dependency installation so @netlify/blobs is available to the functions.
  command = "npm install"

[functions]
  node_bundler = "esbuild"
